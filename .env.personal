# PERSONAL USE ONLY — do not commit this file, do not expose this mode to
# other users of video-to-prompt. See anthropic_provider.py module docstring.
#
# Fill in the token from `claude setup-token` (same one you already generated
# for personal_run.py — reuse it, don't generate a new one).

VAP_AUTH_MODE=api_key
CLAUDE_CODE_OAUTH_TOKEN=
