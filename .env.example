# Copy this file to .env and fill in your key, or export it directly in your shell.
# Required only if using the LLM reasoning layer (i.e. not running with --no-llm).
ANTHROPIC_API_KEY=sk-ant-your-key-here
